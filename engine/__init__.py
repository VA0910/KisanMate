"""Deterministic engine (PROJECT_SPEC.md layer 1): crop scoring, prior table, fusion, alerts.

Pure Python. Nothing in this package makes an AI or network call, so it must keep
working exactly as-is even when Gemini is completely down.
"""
